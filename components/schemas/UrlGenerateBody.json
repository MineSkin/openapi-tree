{
  "type": "object",
  "description":"URL Generate Request Body",
  "allOf": [
    {
      "$ref": "./GenerateOptions.json"
    },
    {
      "required": [
        "url"
      ],
      "properties": {
        "url": {
          "description": "URL to a PNG image. Also supports data URLs (base64-encoded images).",
          "type": "string"
        }
      },
      "example": {
        "url": "https://i.imgur.com/kQPrtYw.png"
      }
    }
  ]
}