{
  "type": "object",
  "description": "Result of a single item in a batch generate request. Each item is processed independently, so some may succeed while others fail.",
  "required": [
    "success"
  ],
  "properties": {
    "success": {
      "type": "boolean",
      "description": "Whether this item was successfully submitted/generated"
    },
    "job": {
      "$ref": "./JobInfo.json"
    },
    "skin": {
      "$ref": "./SkinInfo.json"
    },
    "errors": {
      "type": "array",
      "description": "Errors for this item, if it failed",
      "items": {
        "$ref": "./CodeAndMessage.json"
      }
    }
  }
}
