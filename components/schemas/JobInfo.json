{
  "type": "object",
  "required": [
    "id",
    "status"
  ],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Job ID"
    },
    "status": {
      "type": "string",
      "enum": [
        "unknown",
        "waiting",
        "active",
        "failed",
        "completed"
      ]
    }
  }
}