{
  "type": "object",
  "required": [
    "next",
    "delay",
    "limit"
  ],
  "properties": {
    "next": {
      "$ref": "./NextRequest.json"
    },
    "delay": {
      "$ref": "./DelayInfo.json"
    },
    "limit": {
      "allOf": [
        {
          "$ref": "./PerWindowLimitInfo.json"
        },
        {
          "type": "object",
          "required": [
            "limit",
            "remaining"
          ],
          "properties": {
            "limit": {
              "deprecated": true,
              "type": "number",
              "description": "Deprecated: Use the fields in 'minute' and 'hour' instead to get information about the individual rate limits.",
              "example": 60
            },
            "remaining": {
              "deprecated": true,
              "type": "number",
              "description": "Deprecated: Use the fields in 'minute' and 'hour' instead to get information about the individual rate limits.",
              "example": 58
            },
            "used": {
              "deprecated": true,
              "type": "number",
              "description": "Deprecated: Use the fields in 'minute' and 'hour' instead to get information about the individual rate limits.",
              "example": 2
            },
            "reset": {
              "deprecated": true,
              "type": "number",
              "description": "Deprecated: Use the fields in 'minute' and 'hour' instead to get information about the individual rate limits.",
              "example": 1746869295
            }
          }
        }
      ]
    }
  }
}