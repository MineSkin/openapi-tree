{
  "type": "object",
  "required": [
    "limit",
    "remaining"
  ],
  "description": "Information about the rate limit for the current request.",
  "properties": {
    "limit": {
      "type": "number",
      "description": "Limit of requests in the current window (1 minute/1 hour).",
      "example": 60
    },
    "remaining": {
      "type": "number",
      "description": "Remaining requests in the current window.",
      "example": 58
    },
    "used": {
      "type": "number",
      "description": "Number of requests used in the current window.",
      "example": 2
    },
    "reset": {
      "type": "number",
      "description": "UNIX epoch timestamp (seconds) when the rate limit will reset.",
      "example": 1746869295
    }
  }
}