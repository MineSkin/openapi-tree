{
  "type": "object",
  "required": [
    "millis"
  ],
  "description": "Information about the delay between requests. These numbers will remain the same for all requests with the same API key. You can use this to send requests on a schedule, for example.",
  "properties": {
    "millis": {
      "type": "number",
      "description": "Delay between requests (in milliseconds) - depends on the API key used for this request",
      "example": 1200
    },
    "seconds": {
      "type": "number",
      "description": "Delay between requests (in seconds) - depends on the API key used for this request",
      "example": 1.2
    }
  }
}