{
  "type": "object",
  "required": [
    "absolute",
    "relative"
  ],
  "description": "Information about when the next request can be made. Use this to determine when to make the next request.",
  "properties": {
    "absolute": {
      "type": "number",
      "description": "Absolute timestamp (in milliseconds) after which the next request can be made",
      "example": 1726829503123
    },
    "relative": {
      "type": "number",
      "description": "Relative delay (in milliseconds) after which the next request can be made",
      "example": 800
    }
  }
}